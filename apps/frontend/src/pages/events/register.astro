---
import { Button } from "$lib/components/ui/button";
import Layout from "../../layouts/Layout.astro";
import { registerEvent } from "../../lib/server/registerEvent";
import type { CreateEventData } from "../../lib/types/event";

let result: { success: boolean; error?: string; event?: any } | null = null;

if (Astro.request.method === 'POST') {
  try {
    const formData = await Astro.request.formData();
    const eventData: CreateEventData = {
      title: formData.get('title') as string,
      description: formData.get('description') as string,
      location: formData.get('location') as string,
      start_time: formData.get('start_time') as string,
      end_time: formData.get('end_time') as string,
    };

    const cookieHeader = Astro.request.headers.get('cookie');
    result = await registerEvent(eventData, cookieHeader);

    if (result.success) {
      return Astro.redirect('/events?created=true');
    }
  } catch (error) {
    console.error('Form processing error:', error);
    result = {
      success: false,
      error: 'An unexpected error occurred. Please try again.'
    };
  }
}
---

<Layout>
  <div class="max-w-4xl mx-auto px-4 py-8">
    <div class="bg-card rounded-xl shadow-lg overflow-hidden">
      <form method="POST" class="p-8">
        {result && !result.success && (
          <div class="bg-destructive/10 border border-destructive/30 rounded-lg p-4 mb-6">
            <p class="text-destructive text-sm m-0">{result.error}</p>
          </div>
        )}
          <div class="bg-secondary-foreground text-primary-foreground p-8 -m-8 mb-8 rounded-t-xl">
          <h2 class="text-3xl font-bold mb-3">Host Your Event with Us</h2>
          <p class="text-lg mb-2 opacity-95">Organize. Empower youth. Build community.</p>
          <p class="mb-6 leading-relaxed opacity-90">Submit your student-led event and we'll help you reach a wider audience, connect with peers, and make a measurable impact.</p>

          <h3 class="text-xl font-semibold mt-8 mb-3 pb-2 border-b-2 border-primary-foreground/30">Why Submit Your Event?</h3>
          <ul class="space-y-2 ml-6 list-disc">
            <li>Featured on our platform Event Hub</li>
            <li>Convenient registration system for potential participants</li>
            <li>Optional partnerships and co-promotion when available</li>
          </ul>

          <h3 class="text-xl font-semibold mt-8 mb-3 pb-2 border-b-2 border-primary-foreground/30">Eligibility Criteria</h3>
          <p class="mb-3">Your event should:</p>
          <ul class="space-y-2 ml-6 list-disc">
            <li>Be organized by students or a youth-centered organization.</li>
            <li>Aim to empower, educate, or engage your community.</li>
            <li>Be inclusive and welcoming to participants from diverse backgrounds.</li>
            <li>Not promote hate speech, harassment, or discrimination of any kind.</li>
            <li>Be free or low-cost for attendees.</li>
            <li>Include a clear plan (location, date, agenda, and target audience).</li>
          </ul>

          <h3 class="text-xl font-semibold mt-8 mb-3 pb-2 border-b-2 border-primary-foreground/30">Submission Guidelines</h3>
          <p class="mb-3">To ensure a smooth review:</p>
          <ul class="space-y-2 ml-6 list-disc">
            <li>Complete the form accurately and thoroughly.</li>
            <li>Attach your event plan and any promotional materials through links (flyers, slide deck, social posts).</li>
            <li>Provide a reliable contact person for follow-up.</li>
            <li>Submit at least 1 month before your event date (earlier you submit, longer it will be featured).</li>
          </ul>

          <h3 class="text-xl font-semibold mt-8 mb-3 pb-2 border-b-2 border-primary-foreground/30">What Happens Next</h3>
          <ul class="space-y-2 ml-6 list-disc">
            <li>Within 7 days: We review your submission.</li>
            <li>If approved: We'll email you a confirmation, request any missing details.</li>
            <li>If edits are needed: We'll share feedback so you can update and resubmit.</li>
          </ul>

          <h3 class="text-xl font-semibold mt-8 mb-3 pb-2 border-b-2 border-primary-foreground/30">FAQ</h3>
          <ul class="space-y-2 ml-6 list-disc">
            <li><strong class="font-semibold">Can my event be virtual?</strong> Yes—just include the platform and access link.</li>
            <li><strong class="font-semibold">What if I don't have promo materials yet?</strong> Submit anyway; you can add assets later.</li>
            <li><strong class="font-semibold">Is there a fee to be listed?</strong> No. Listing is free for qualifying events.</li>
            <li><strong class="font-semibold">How soon will I hear back?</strong> Usually within 7 days.</li>
            <li><strong class="font-semibold">Do you provide funding?</strong> Not at this time. We focus on visibility and connections.</li>
            <li><strong class="font-semibold">Can we use your logo?</strong> With approval. We'll provide brand guidelines if applicable.</li>
          </ul>

          <h3 class="text-xl font-semibold mt-8 mb-3 pb-2 border-b-2 border-primary-foreground/30">Community Standards</h3>
          <p class="opacity-90">We uphold a zero-tolerance policy for harassment, hate, or discrimination. Events must maintain a safe, respectful, and inclusive environment for all participants.</p>

          <h3 class="text-xl font-semibold mt-8 mb-3 pb-2 border-b-2 border-primary-foreground/30">Need Help?</h3>
          <p class="opacity-90">Questions before you submit? Email us at <a href="mailto:youthunite89@gmail.com" class="text-[var(--orange-light)] underline font-medium hover:text-primary transition-colors">youthunite89@gmail.com</a> and we'll get back to you promptly.</p>
        </div>
        <div class="mb-6">
          <label for="title" class="block font-medium text-card-foreground mb-2">Event Title *</label>
          <input
            type="text"
            id="title"
            name="title"
            placeholder="Enter event title"
            maxlength="100"
            class="w-full px-4 py-3 border border-input rounded-lg focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all"
            required
          />
        </div>

        <div class="mb-6">
          <label for="description" class="block font-medium text-card-foreground mb-2">Description *</label>
          <textarea
            id="description"
            name="description"
            placeholder="Describe your event..."
            rows="4"
            class="w-full px-4 py-3 border border-input rounded-lg focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all resize-vertical min-h-[100px]"
            required
          ></textarea>
        </div>

        <div class="mb-6">
          <label for="location" class="block font-medium text-card-foreground mb-2">Location *</label>
          <input
            type="text"
            id="location"
            name="location"
            placeholder="Event location"
            maxlength="255"
            class="w-full px-4 py-3 border border-input rounded-lg focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all"
            required
          />
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mb-8">
          <div>
            <label for="start_time" class="block font-medium text-card-foreground mb-2">Start Time *</label>
            <input
              type="datetime-local"
              id="start_time"
              name="start_time"
              class="w-full px-4 py-3 border border-input rounded-lg focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all"
              required
            />
          </div>

          <div>
            <label for="end_time" class="block font-medium text-card-foreground mb-2">End Time *</label>
            <input
              type="datetime-local"
              id="end_time"
              name="end_time"
              class="w-full px-4 py-3 border border-input rounded-lg focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all"
              required
            />
          </div>
        </div>

        <div class="flex gap-4 justify-end">
          <a href="/events"><Button variant="secondary">Cancel</Button></a>
          <Button type="submit">Create Event</Button>
        </div>
      </form>
    </div>
  </div>
</Layout>

<script>
  const now = new Date();
  const nextHour = new Date(now.getTime() + 60 * 60 * 1000);
  nextHour.setMinutes(0, 0, 0);
  const twoHoursLater = new Date(nextHour.getTime() + 2 * 60 * 60 * 1000);

  const startTimeInput = document.getElementById('start_time') as HTMLInputElement;
  const endTimeInput = document.getElementById('end_time') as HTMLInputElement;

  if (startTimeInput && endTimeInput) {
    startTimeInput.value = nextHour.toISOString().slice(0, 16);
    endTimeInput.value = twoHoursLater.toISOString().slice(0, 16);

    startTimeInput.addEventListener('change', () => {
      const startTime = new Date(startTimeInput.value);
      const endTime = new Date(startTime.getTime() + 2 * 60 * 60 * 1000);
      endTimeInput.value = endTime.toISOString().slice(0, 16);
    });
  }
</script>
